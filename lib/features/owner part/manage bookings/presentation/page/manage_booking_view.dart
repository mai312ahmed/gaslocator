import "package:flutter/material.dart";
import "package:gaslocator/features/owner%20part/manage%20bookings/presentation/widgets/manage_booking_view_body.dart";

class ManageBookingView extends StatelessWidget {
  const ManageBookingView({super.key});
  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: ManageBookingViewBody(),
    );
  }
}
